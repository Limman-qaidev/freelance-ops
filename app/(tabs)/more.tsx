import { ScreenShell } from '@/ui/components/screen-shell';

export default function MoreScreen() {
  return (
    <ScreenShell
      title="More"
      subtitle="Settings, activities, exports, backup and restore will be added through their dedicated V1 slices."
    />
  );
}

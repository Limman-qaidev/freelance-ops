import { ScreenShell } from '@/ui/components/screen-shell';

export default function PlanningScreen() {
  return (
    <ScreenShell
      title="Planning"
      subtitle="Calendar and simplified timeline views arrive in the Project Control slice."
    />
  );
}
